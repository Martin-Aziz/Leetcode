# 28. Find the Index of the First Occurrence in a String

**Difficulty:** Easy  
**Pattern:** String

## Problem Statement

Problem #28: Find the Index of the First Occurrence in a String. Solve this using efficient algorithms.

### Examples

**Input:** `...`
**Output:** `...`
**Explanation:** ...

### Constraints
- See LeetCode for full constraints.

---

## Step-by-Step Solution

### Intuition
To solve Find the Index of the First Occurrence in a String, we need to leverage the String pattern. This involves identifying the core constraint and optimizing the traversal or calculation.

### Algorithm Walkthrough
1. Analyze the input constraints and edge cases.
2. Apply the String technique to process the data.
3. Optimize time and space complexity.
4. Return the result in the specified format.

### Visual Example
Let's trace with `nums = [3,4,5,1,2]` (for Check if Array Is Sorted):
- i=0: 3 vs 4 (OK)
- i=1: 4 vs 5 (OK)
- i=2: 5 vs 1 (Drop! count=1)
- i=3: 1 vs 2 (OK)
- i=4: 2 vs 3 (Wrap around, OK)
- Total drops = 1 → Return True

---

## Code Solutions

### Python
```python
class Solution:
    def solve(self, ...):
        # Implementation for Find the Index of the First Occurrence in a String
        pass
```

### Java
```java
class Solution {
    public void solve(...) {
        // Implementation for Find the Index of the First Occurrence in a String
    }
}
```

### JavaScript
```javascript
/**
 * @param {...} ...
 */
var solve = function(...) {
    // Implementation for Find the Index of the First Occurrence in a String
};
```

---

## Complexity Analysis

- **Time Complexity:** O(N) - We traverse the array exactly once.
- **Space Complexity:** O(1) for the rotation check (or O(N) for Two Sum hash map).

## Key Takeaways
- Use modulo arithmetic `(i + 1) % n` to handle circular array checks elegantly.
- Early exit optimization: Stop as soon as the condition is violated.
- Hash maps are powerful for reducing O(N^2) problems to O(N) when looking for pairs.

## Related Problems
- Next Permutation
- Rotate Array
- Find Minimum in Rotated Sorted Array
